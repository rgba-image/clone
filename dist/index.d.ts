import { CreateImage } from '@rgba-image/common';
export declare const CloneFactory: (createImage: CreateImage) => ({ width, height, data }: ImageData) => ImageData;
export declare const clone: ({ width, height, data }: ImageData) => ImageData;
